<UserControl x:Class="SoftwareFromClick.Views.DynamicGeneratorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border x:Name="Loading" 
                Background="#CC000000" 
                Visibility="Collapsed"
                Grid.RowSpan="4"
                Panel.ZIndex="100">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Sending request to AI model..." 
                           Foreground="White" 
                           FontSize="18" 
                           FontWeight="Bold"
                           Margin="0,0,0,20"/>
                <TextBlock Text="Please wait for response..." 
                           Foreground="White" 
                           FontSize="14"
                           TextAlignment="Center"/>
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="0" x:Name="HeaderTextBlock" Text="Custom Generator" FontSize="20" FontWeight="Bold" Margin="20,20,0,0"/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="20">
            <StackPanel x:Name="DynamicFormPanel">
                </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Bottom"
                    Margin="0,10,0,20">
            <Border BorderBrush="Black" BorderThickness="1" Margin="0,0,10,0">
                <Button Content="Back" Width="100" Height="35" BorderThickness="0" Click="BackButton_Click"/>
            </Border>
            <Border BorderBrush="Black" BorderThickness="1">
                <Button Content="Generate Code" Width="120" Height="35" BorderThickness="0" Click="GenerateCodeButton_Click"/>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>