<Window x:Class="SoftwareFromClick.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings - API Keys" Height="500" Width="700">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="API Keys Configuration" FontSize="18" FontWeight="Bold" Margin="20"/>

        <!-- Add new key form -->
        <Grid Grid.Row="1" Margin="20,0,20,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <TextBlock Text="Provider:" Margin="0,0,0,5"/>
                <Border BorderBrush="Black" BorderThickness="1">
                    <ComboBox x:Name="ProviderComboBox" Height="22" BorderThickness="0">
                        <ComboBoxItem Content="OpenAI (GPT)"/>
                    </ComboBox>
                </Border>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="0,0,10,0">
                <TextBlock Text="API Key:" Margin="0,0,0,5"/>
                <Border BorderBrush="Black" BorderThickness="1">
                    <TextBox x:Name="KeyTextBox" Height="22" BorderThickness="0"/>
                </Border>
            </StackPanel>

            <Border Grid.Column="2" BorderBrush="Black" BorderThickness="1" VerticalAlignment="Bottom">
                <Button Content="Add" Width="80" Height="22" BorderThickness="0" Click="AddButton_Click"/>
            </Border>
        </Grid>

        <!-- API Keys List -->
        <Border Grid.Row="2" BorderBrush="Black" BorderThickness="1" Margin="20,0,20,20">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <DataGrid x:Name="KeysDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Provider" Binding="{Binding Provider.Name}" Width="150"/>
                        <DataGridTextColumn Header="API Key" Binding="{Binding ApiKey}" Width="*"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding IsActive}" Width="100"/>

                        <DataGridTemplateColumn Header="Actions" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Delete" Width="70" Click="DeleteButton_Click" Tag="{Binding Id}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20">
            <Border BorderBrush="Black" BorderThickness="1">
                <Button Content="Close" Width="80" Height="30" BorderThickness="0" Click="CloseButton_Click"/>
            </Border>
        </StackPanel>

        <!-- Bottom buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20">
            <Border BorderBrush="Black" BorderThickness="1" Margin="0,0,10,0">
                <Button Content="Cancel" Width="80" Height="30" BorderThickness="0" Click="CloseButton_Click"/>
            </Border>
        </StackPanel>
    </Grid>
</Window>