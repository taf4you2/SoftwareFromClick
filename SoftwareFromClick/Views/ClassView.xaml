<UserControl x:Class="SoftwareFromClick.Views.ClassView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">

            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border x:Name="Loading" 
                        Background="#CC000000" 
                        Visibility="Collapsed"
                        Grid.RowSpan="15" 
                        Panel.ZIndex="100">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="Generating Class..." 
                                   Foreground="White" 
                                   FontSize="18" 
                                   FontWeight="Bold"
                                   Margin="0,0,0,20"/>
                        <TextBlock Text="Please wait..." 
                                   Foreground="White" 
                                   FontSize="14"
                                   TextAlignment="Center"/>
                    </StackPanel>
                </Border>

                <TextBlock Grid.Row="0" Text="Define Class" FontSize="20" FontWeight="Bold"/>

                <StackPanel Grid.Row="2">
                    <TextBlock Text="Class Name:" Margin="0,0,0,5"/>
                    <Border BorderBrush="Black" BorderThickness="1">
                        <TextBox x:Name="ClassNameTextBox" Height="30" BorderThickness="0" ToolTip="Enter class name, e.g. OrderManager"/>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Row="4">
                    <TextBlock Text="Access Modifier:" Margin="0,0,0,5"/>
                    <Border BorderBrush="Black" BorderThickness="1">
                        <ComboBox x:Name="AccessModifierComboBox" Height="30" BorderThickness="0" SelectedIndex="0">
                            <ComboBoxItem Content="public"/>
                            <ComboBoxItem Content="internal"/>
                            <ComboBoxItem Content="public static"/>
                            <ComboBoxItem Content="public abstract"/>
                        </ComboBox>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Row="8">
                    <TextBlock Text="Properties &amp; Fields:" Margin="0,0,0,5"/>
                    <Border BorderBrush="Black" BorderThickness="1">
                        <TextBox x:Name="PropertiesTextBox"
                                 Height="80" 
                                 AcceptsReturn="True" 
                                 TextWrapping="Wrap" 
                                 VerticalScrollBarVisibility="Auto"
                                 BorderThickness="0"
                                 ToolTip="e.g. int Id, string Name { get; set; }"/>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Row="10">
                    <TextBlock Text="Methods &amp; Logic Description:" Margin="0,0,0,5"/>
                    <Border BorderBrush="Black" BorderThickness="1">
                        <TextBox x:Name="MethodsTextBox"
                                 Height="100" 
                                 AcceptsReturn="True" 
                                 TextWrapping="Wrap" 
                                 VerticalScrollBarVisibility="Auto"
                                 BorderThickness="0"
                                 ToolTip="Describe methods and behavior of the class"/>
                    </Border>
                </StackPanel>

                <Border x:Name="PromptPreviewPanel" 
                        Grid.Row="12" 
                        BorderBrush="Black" 
                        BorderThickness="1" 
                        Visibility="Collapsed"
                        Padding="10">
                    <StackPanel>
                        <TextBlock Text="Prompt Preview:" FontWeight="Bold" Margin="0,0,0,5"/>
                        <Border BorderBrush="Black" BorderThickness="1">
                            <TextBox x:Name="PromptPreviewTextBox"
                                     Height="150" 
                                     AcceptsReturn="True" 
                                     TextWrapping="Wrap" 
                                     VerticalScrollBarVisibility="Auto"
                                     BorderThickness="0"
                                     IsReadOnly="False"
                                     Background="White"/>
                        </Border>
                    </StackPanel>
                </Border>

                <StackPanel Grid.Row="14" 
                            Orientation="Horizontal" 
                            HorizontalAlignment="Center" 
                            VerticalAlignment="Bottom"
                            Margin="0,20,0,0">
                    <Border BorderBrush="Black" BorderThickness="1" Margin="0,0,10,0">
                        <Button Content="Back" Width="100" Height="35" BorderThickness="0" Click="BackButton_Click"/>
                    </Border>
                    <Border BorderBrush="Black" BorderThickness="1" Margin="0,0,10,0">
                        <Button Content="Preview Prompt" Width="120" Height="35" BorderThickness="0" Click="PreviewPromptButton_Click"/>
                    </Border>
                    <Border BorderBrush="Black" BorderThickness="1">
                        <Button Content="Generate Code" Width="120" Height="35" BorderThickness="0" Click="GenerateCodeButton_Click"/>
                    </Border>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>